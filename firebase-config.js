// Paste your Firebase web app config here (Firebase console → Project settings →
// Your apps → SDK setup and configuration → Config). The apiKey is not a secret;
// access is controlled by the Firestore rules in firestore.rules.
//
// Leave apiKey as "PASTE_ME" to run the site in local demo mode (data stays in
// this browser only).
export const firebaseConfig = {
  apiKey: "PASTE_ME",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "000000000000",
  appId: "1:000000000000:web:0000000000000000000000",
};
